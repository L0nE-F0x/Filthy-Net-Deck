# THIS is the config Netlify actually reads — the site's base directory is
# `website`. The repo-root netlify.toml is inert; see the warning at the top of
# it. Put all Netlify config here.

[build]
  publish = "."
  # Optional: run pipeline before publish when wired to CI
  # command = "node ../pipeline/build-meta.mjs --seed"

# NOTE: install counting (docs/INSTALL-COUNTING.md) is NOT wired up here.
# A first attempt redirected /version.json to a Netlify Function and took the
# endpoint down with a 404: the redirect deployed but the function did not,
# because this site's base directory is `website` and there is no package.json
# there for Netlify to install @netlify/blobs from. Do not re-add the redirect
# until the function is confirmed deployed at /.netlify/functions/version.

# Force UTF-8 so em dashes / curly quotes never mojibake in browsers
[[headers]]
  for = "/*.html"
  [headers.values]
    Content-Type = "text/html; charset=UTF-8"

[[headers]]
  for = "/"
  [headers.values]
    Content-Type = "text/html; charset=UTF-8"

# CORS so the Tauri desktop app can fetch meta + version.json
[[headers]]
  for = "/meta/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, HEAD, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type, Accept"
    Cache-Control = "public, max-age=120"

[[headers]]
  for = "/version.json"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, HEAD, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type, Accept"
    Cache-Control = "public, max-age=60"

[[headers]]
  for = "/downloads/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Cache-Control = "public, max-age=3600"
