export const APP_VERSION = "0.15.0";
export const APP_NAME = "Filthy Net Deck";
export const APP_SLUG = "filthy-net-deck";

/**
 * Player-facing highlights for THIS version — shown once after an update
 * installs (see WhatsNew in StatusBanners). Update alongside APP_VERSION.
 */
export const WHATS_NEW: string[] = [
  "Start with your PC: optional tray autostart so matches are always tracked (Settings)",
  "The app remembers your window size and last format",
  "First close now explains the app keeps running in the tray",
];
