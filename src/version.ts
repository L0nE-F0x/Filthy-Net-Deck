export const APP_VERSION = "0.24.0";
export const APP_NAME = "Filthy Net Deck";
export const APP_SLUG = "filthy-net-deck";

/**
 * Player-facing highlights for THIS version — shown once after an update
 * installs (see WhatsNew in StatusBanners). Update alongside APP_VERSION.
 */
export const WHATS_NEW: string[] = [
  "Fullscreen controls live in the top bar — Exit fullscreen and Close to tray no longer cover Search or the theme toggle",
  "Search — the top-bar chip is a clear Search button (Ctrl+K still works)",
  "Recently live — Sets now shows the full current Standard pool (Foundations through the latest Arena drop), not just Marvel",
  "Settings — two-column layout with compact notification toggles, so prefs fill the page instead of a long single list",
];
